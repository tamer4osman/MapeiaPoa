idea {
  module {
    excludeDirs += files(
        'build',
        '.git',
        '.gradle',
        'gradle'
    )
  }

  project {
    jdkName = '1.8'
    languageLevel = '1.8'
    vcs = 'Git'
  }
}
